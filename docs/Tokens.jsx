import React from "react";

// import tokenData from "../src/styles/tokens/tokens-json.json";

const Tokens = () => {
  return (
    <div>
      {/* {Object.entries(tokenData.color.neutral).map(entry => {
        return (
          <div>
            <pre>{JSON.stringify(entry, null, 2)}</pre>
          </div>
        );
      })} */}
    </div>
  );
};

export default Tokens;
